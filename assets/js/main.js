$(document).ready(function () {
  $(".cont_slick").slick({
    dots: true, // Включение точек для навигации
    autoplay: true,
    autoplaySpeed: 3000,
    arrows: false, // Выключение стрелок
  });
});
